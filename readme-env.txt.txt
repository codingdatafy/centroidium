# CodingDatafy - Environment Variables Template
# Note: Local environment files (.env*.local) are excluded from git tracking.

# === GitHub API Integration ===
# Private Personal Access Token (PAT) with read permissions for organization repositories.
# This token is strictly consumed on the server side (Server-only runtime).
ORGANIZATION_GITHUB_TOKEN=github_personal_access_token